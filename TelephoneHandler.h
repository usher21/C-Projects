#ifndef __TELEPHONE__HANDLER__H__
#define __TELEPHONE__HANDLER__H__

	#include <stdbool.h>

	void deleteSpaces(char *str);
	bool isValidNum(char *num);
	bool isNumString(char *num);
	bool isNum(char car);
	bool isValidIntial(char *num);
	char *formatNum(char *num);

#endif